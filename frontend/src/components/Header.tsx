import { FunctionalComponent } from "preact";

const Header: FunctionalComponent = () => {
  return (
    <header className="container p-4">
      <a href="/" className="font-extrabold text-xl">🏆 Trophies.gg</a>
    </header>
  );
};

export default Header;
